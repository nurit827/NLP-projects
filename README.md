# NLP Projects

## Setup

```bash
python3 -m venv nlp
source nlp/bin/activate
pip install -r requirements.txt
python -m spacy download en_core_web_sm
```

Then launch Jupyter: `jupyter notebook` and open the notebook in the relevant exercise folder (e.g. `ex1/ex1.ipynb`).
